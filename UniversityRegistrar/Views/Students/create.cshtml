@{
  Layout = "_Layout";
}

@model UniversityRegistrar.Models.Student

<h4>Add a new Student</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name, new { @required = "required"})
  @Html.LabelFor(model => model.DateOfEnrollment)
  @Html.TextBoxFor(model => model.DateOfEnrollment, new { @required = "required"})

  @* @Html.Label("Select course")
  @Html.DropDownList(" CourseId") *@
  <input type="submit" value="Add new student" class="btn btn-dark" />
}

<p>@Html.ActionLink("Show all students", "Index")</p>